name: volvo-hmi
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
    - openai>=1.0.0
    - requests>=2.28.0
    - PyQt6>=6.4.0
    - numpy>=1.24.0
    # Optional STT dependencies (install separately on target hardware)
    # - pyaudio
    # - faster-whisper
